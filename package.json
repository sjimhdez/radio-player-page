{
  "name": "radio-player-page-plugin",
  "version": "2.0.2",
  "private": true,
  "description": "WordPress plugin for dedicated radio player pages",
  "scripts": {
    "prepare": "husky install",
    "lint:player": "cd player && npm run lint",
    "test:php-syntax": "bash scripts/php-syntax-check.sh",
    "test:plugin-check": "bash scripts/plugin-check.sh",
    "test:all": "npm run lint:player && npm run test:php-syntax && npm run test:plugin-check"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "engines": {
    "node": ">=20"
  }
}
